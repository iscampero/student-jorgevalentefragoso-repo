apiVersion: v1
kind: Service
metadata:
  name: student-israel-app-service
spec:
  selector:
    app: student-israel-app
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
